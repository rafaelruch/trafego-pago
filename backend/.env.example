# === BANCO DE DADOS ===
DATABASE_URL=postgresql://postgres:senha@db:5432/gestor_trafego

# === META / FACEBOOK ===
META_APP_ID=1008535358112208
META_APP_SECRET=seu_app_secret_aqui
# URL de callback OAuth — deve ser o backend (API)
META_REDIRECT_URI=https://apitrafego.ruch.com.br/api/auth/callback

# === ANTHROPIC (Claude AI) ===
# Crie sua chave em: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# === SEGURANÇA ===
# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-por-uma-chave-secreta-forte-aqui

# === CORS e FRONTEND ===
# Domínios permitidos pelo backend
CORS_ORIGINS=["https://trafego.ruch.com.br"]
# URL do frontend (para redirect pós-login OAuth)
FRONTEND_URL=https://trafego.ruch.com.br

# === AMBIENTE ===
# development | production
ENVIRONMENT=production
